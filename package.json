{
  "name": "turbofy",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose up -d --build",
    "build": "pnpm run backend:build",
    "backend:build": "pnpm -C backend build",
    "frontend:build": "pnpm -C frontend build",
    "infra:build": "pnpm exec sst build",
    "deploy": "pnpm exec sst deploy",
    "remove": "pnpm exec sst remove"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "sst": "^2.30.2"
  }
}
